import { Box } from '@mui/material';
import React from 'react';

const Loader = () => {
  return (
    <Box>
      <img src='/gif/ripple.gif' alt='Loader' />
    </Box>
  );
};

export default Loader;
